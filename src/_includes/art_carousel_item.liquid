<div class="artist-carousel-item">
  <img src="{{ art.img }}" alt="{{ art.title }}">
  <div class="artist-carousel-caption">
    <span>{{ art.title }}</span>{% if art.country %}<span class="country">({{ art.country }})</span>{% endif %}
  </div>
</div>
