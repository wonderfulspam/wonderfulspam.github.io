<li class="desktop">
  <a class="artist-link" data-slug="{{ art.slug }}">
    <div>
      <span>{{ art.title }}{% if art.country %}</span><span class="country">({{ art.country }}){% endif %}</span>
    </div>
    <img src="{{ art.img }}">
  </a>
</li>
