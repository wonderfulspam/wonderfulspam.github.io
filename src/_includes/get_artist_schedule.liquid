{% comment %}
This include finds the scheduled day and time for an artist.
Input: artist_title (the artist's title to search for)
Output: artist_day (THU/FRI/SAT), artist_time (HH:MM)
{% endcomment %}

{% assign artist_day = "" %}
{% assign artist_time = "" %}

{% for event in site.data.events %}
  {% for day in event.days %}
    {% for slot in day.schedule %}
      {% if slot.artist == artist_title %}
        {% assign artist_time = slot.time %}
        {% if day.name == "Thursday" %}
          {% assign artist_day = "THU" %}
        {% elsif day.name == "Friday" %}
          {% assign artist_day = "FRI" %}
        {% elsif day.name == "Saturday" %}
          {% assign artist_day = "SAT" %}
        {% endif %}
      {% endif %}
    {% endfor %}
  {% endfor %}
{% endfor %}
